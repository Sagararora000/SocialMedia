<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=locals.postDisplay%>
    </title>

    <!-- Used to style Layout like header,footer in all pages -->
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <!-- Used to style elements in indivdual pages -->
    <%- style %>

</head>

<body>
    <%- include('_header'); %>

        <% if(flash.success && flash.success.length> 0){%>
            <h5>
                <%= flash.success %>
            </h5>
        <% } %>
                <div id="layout-main">
                    <%- body %>
                </div>

                <%- include('_footer');%>

                    <!-- Used to style in script in individual pages -->
                    <%- script %>
                        <script src="/js/header.js"></script>
</body>

</html>